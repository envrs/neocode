{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["CI", "NEOCODE_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "NEOCODE_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "neocode#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@neocode-ai/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
